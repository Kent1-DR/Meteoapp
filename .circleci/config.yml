# Couldn't automatically generate a config from your source code.
# This is a generic template to serve as a base for your custom config

# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/jobs-steps/#jobs-overview & https://circleci.com/docs/configuration-reference/#jobs
jobs:
  test:
    # Specify the execution environment. You can specify an image from Docker Hub or use one of our convenience images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/executor-intro/ & https://circleci.com/docs/configuration-reference/#executor-job
    machine:
      # Specify the version you desire here
      # See: https://circleci.com/developer/images/image/cimg/base
      - image: ubuntu-2204:2022.04.2

    # Add steps to the job
    # See: https://circleci.com/docs/jobs-steps/#steps-overview & https://circleci.com/docs/configuration-reference/#steps
    steps:
      # Checkout the code as the first step.
      - checkout
      # Replace this with a real test runner invocation
      - run:
          name: Update / upgrade and install curl
          command: sudo apt update && sudo apt upgrade && sudo apt install curl
      
      - run:
          name: docker build
          command: docker build -t meteoapp .

      - run:
          name: docker run
          command:  docker run -d -p 5500:5500 meteoapp
  
      - run:
          name: docker wait
          command:  sleep 10

      - run:
          name: test connection
          command:  curl http://127.0.0.1:5500/ 
  
      - run:
          name: connect to dockerhub
          command: echo "$DOCKERPASSWORD" | docker login -u king12quentin --password-stdin

      - run:
          name: upload to dockerhub
          command: docker tag meteoapp "king12quentin/meteoapp" && docker push "king12quentin/meteoapp"


workflows:
  build:
    jobs:
      - test